/**
 * @namespace   Typertext
 * @module      Http
 */
module Typertext.Http {
    /**
     * TODO figure out the proper annotations for enums
     */
    export enum HttpMethod {
        DELETE,
        GET,
        HEAD,
        OPTIONS,
        POST,
        PUT,
        TRACE
    }
}